# Modify Docker daemon configuration.
- name: Configure Docker
  become: true
  copy:
    src: daemon.json
    dest: "{{docker.conf_dir}}/daemon.json"
    owner: root
    group: root
    mode: 0600
  tags:
    - setup

# Override Docker service configuration in Systemd.
- name: Ensure /etc/systemd/system/docker.service.d directory exists
  become: true
  file:
    path: /etc/systemd/system/docker.service.d
    state: directory
    mode: '0755'
  tags:
    - setup

# Create override.conf for Docker service.
- name: Create override.conf for Docker service
  become: true
  template:
    src: override.conf.j2
    dest: /etc/systemd/system/docker.service.d/override.conf
    mode: '0644'
  tags:
    - setup

# Reload systemd to apply the Docker override configuration.
- name: Reload systemd to apply the Docker override configuration
  become: true
  systemd:
    daemon_reload: yes
  tags:
    - setup

# Restart Docker service.
- name: Restart Docker service
  become: true
  systemd:
    name: docker.service
    enabled: yes
    state: restarted
  tags:
    - setup